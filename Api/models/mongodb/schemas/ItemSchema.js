var mongoose = require('mongoose'),
    Schema = mongoose.Schema;

/*var ItemSchema = new Schema({
    name: String,
    type: String,
    userId: Number,
    isRoot: {
        type: Boolean,
        default: false
    },
    systemPath: String,
    shared: {
        type: Boolean,
        default: false
    },
    shareId: {
        type: Schema.ObjectId,
        default: null
    },
    version: Number,
    parents: [{
        itemId: Schema.ObjectId,
        ref: 'Item'
    }],
    items: [{
        itemId: Schema.ObjectId,
        ref: 'Item'
    }],
    date: {
        type: Date,
        default: Date.now
    },
    oldVersions: [{
        versionId: Schema.ObjectId,
        ref: 'Version'
    }]
});*/
